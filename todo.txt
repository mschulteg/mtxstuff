OLD:
- Command methods should not return a vector of arguments??
- builder pattern for command (but it always has to run on the same file)

NEW:
- Quit correctly without artifacts
- Refresh changed files (automatically or via button)
- Improve command generator popup
- Make Frame<CrosstermBackend<Stdout>> generic for popup trait
- Show controls somehwere (at the bottom and/or inside the popups)
- Only generate shell file on request, add option to run the commands
- Change name property in generate_commands
- Handle "unset" for track name and lang
- Variable size message popup