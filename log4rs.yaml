refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
  requests:
    kind: file
    append: false
    path: "log/requests.log"
    encoder:
      pattern: "{d} - {m}{n}"
root:
  level: info
  appenders:
    - requests